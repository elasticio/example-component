const { expect } = require('chai');
const bunyan = require('bunyan');
const ExampleComponentClient = require('../lib/example-componentClient');

const cfg = {};

const emitter = { logger: bunyan.createLogger({ name: 'dummy' }) };

describe('ExampleComponent Client', () => {

});
